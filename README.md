# test-tf

some text

Fork PR test: validating skipped auto-merge job, empty secret on pull_request, and secret access via workflow_run.

Fork PR #2: validating the fork-safe auto-merge flow (Fork Test -> Fork Auto Merge via workflow_run).
